{
  "name": "zmod",
  "version": "0.2.0",
  "description": "Lightning-fast codemod toolkit powered by Rust and SWC",
  "keywords": [
    "ast",
    "codemod",
    "jscodeshift",
    "refactor",
    "rust",
    "swc",
    "transform"
  ],
  "files": [
    "dist",
    "*.node"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:napi": "napi build --platform --release --pipe \"tsc\"",
    "build:debug": "napi build --platform",
    "prepublishOnly": "napi prepublish -t npm",
    "test": "vitest run",
    "artifacts": "napi artifacts"
  },
  "dependencies": {
    "tinyglobby": "^0.2.12"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.5.1",
    "@types/node": "^25.2.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "napi": {
    "name": "zmod",
    "triples": {
      "defaults": true
    }
  }
}
