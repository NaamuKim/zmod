{
  "name": "@zmod-fixtures/react-hooks-migration",
  "version": "0.1.0",
  "private": true,
  "description": "Example: Migrating React class components to hooks using zmod",
  "scripts": {
    "codemod": "tsx codemod.ts",
    "test": "tsx codemod.ts && echo '✅ Codemod completed'",
    "restore": "git checkout src/ || echo 'No git changes to restore'"
  },
  "dependencies": {
    "zmod": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.2.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
